<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="ClipSync.UI.SettingsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="ClipSync Settings">
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Padding="20" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Excluded apps" FontSize="20" FontWeight="SemiBold"/>

        <TextBlock Grid.Row="1" TextWrapping="Wrap"
                   Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                   Text="Items copied while these apps are in the foreground are not sent to your other devices."/>

        <Grid Grid.Row="2">
            <ListView x:Name="ExcludedList" SelectionMode="None"
                      Background="{ThemeResource LayerFillColorDefaultBrush}"
                      BorderBrush="{ThemeResource SystemControlForegroundBaseMediumLowBrush}"
                      BorderThickness="1" CornerRadius="4"/>
            <TextBlock x:Name="EmptyText"
                       Text="No apps excluded. Everything you copy is synced."
                       Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Spacing="8" HorizontalAlignment="Left">
            <Button x:Name="AddButton" Content="Add app…" Click="OnAddApp"/>
            <!-- Captures through the same resolver the clipboard watcher
                 consults, so what it stores is by construction what
                 copy-time matching will see. The picker keys a Win32 app on
                 its Start Menu shortcut's target, which for launcher-based
                 apps is not the executable that owns the foreground window. -->
            <Button x:Name="CaptureButton" Content="Exclude current app…"
                    Click="OnCaptureCurrentApp"/>
        </StackPanel>

        <TextBlock Grid.Row="4" x:Name="CaptureStatus" TextWrapping="Wrap" Text=""
                   Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
    </Grid>
</Window>
